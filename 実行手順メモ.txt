ーー実行した内容をコミットするたびにメモーー
見方
・コミットメッセージ名
実行した内容（コミットするまでのコマンド実行・ソース記述や処理等）
※コミット毎なので、以下の内容が都度更新され、前のコミットは削除される。（コミット時に記述忘れの場合は、次コミット時にまとめて記述）

・Postgresql接続確認

src/.envファイル作成し、設定値を記述
src/db.tsファイル作成し、接続情報などを記述

必要パッケージ（モジュール）をインストール
npm install pg dotenv
npm install --save-dev @types/pg

以下をターミナル実行
npx ts-node src/index.ts

失敗が起きたため、原因追及。
原因：src/.envファイルの位置が違った。プロジェクト直下に.envを移動。

