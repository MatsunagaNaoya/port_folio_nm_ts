ーー実行した内容をコミットするたびにメモーー
見方
・コミットメッセージ名
実行した内容（コミットするまでのコマンド実行・ソース記述や処理等）
※コミット毎なので、以下の内容が都度更新され、前のコミットは削除される。（コミット時に記述忘れの場合は、次コミット時にまとめて記述）


・ローカルスクリプト・テンプレート整理、不要ファイルのold退避、役割コメント追記
コミットメッセージ: "chore: ローカルスクリプト・テンプレート整理、不要ファイルのold退避、役割コメント追記"

実行した内容:
1. local/ ディレクトリ内のスクリプトを用途ごとに分類
   - 不要・一時退避用は local/old/ へ移動
   - 汎用テンプレートは local/template/ 配下（migration/analysis/ops）へ移動
2. 各スクリプトの先頭に役割コメントを日本語で追記
3. ディレクトリ構成を整理し、運用ルールを明確化

実行コマンド:
# ディレクトリ作成
mkdir local/old
mkdir local/template
mkdir local/template/migration
mkdir local/template/analysis
mkdir local/template/ops

# ファイル移動
mv local/migration.ts local/old/
mv local/test-new-table.ts local/old/
mv local/db-info.ts local/old/
mv local/migrate-belt-tables.ts local/template/migration/
mv local/migrate-belt-data.ts local/template/migration/
mv local/insert-belt-ids.ts local/template/migration/
mv local/cleanup-belt-tables.ts local/template/ops/
mv local/belt-effect-analyzer.ts local/template/analysis/
mv local/belt-data-analysis.ts local/template/analysis/

改善結果:
- スクリプトの役割が明確化され、再利用・保守が容易に
- 不要ファイルの退避でlocal/が整理され、運用ルールも明確に


・プロジェクト全体の改善とパラメータ設定の柔軟化
コミットメッセージ: "feat: プロジェクト全体の改善とパラメータ設定の柔軟化"

実行した内容:
1. データベース接続・操作の改善 (common/interface/db.ts)
   - 重複コードの削除（transformNamedParams関数の統合）
   - 型安全性の向上（DatabaseConfig, QueryResult型の追加）
   - 環境変数の検証機能追加（validateEnvVars関数）
   - エラーハンドリングの改善（async/await対応）
   - 不要なデバッグコードの削除

2. メインアプリケーションの改善 (src/index.ts)
   - 材料を1～5種類まで対応するSQLクエリに改良
   - データ構造の改善（MaterialInfo, DishData型の再設計）
   - パラメータ設定の柔軟化（コマンドライン引数、環境変数、デフォルト値の優先順位）
   - 関数の分離（fetchDishData, displayDishData, getDishData）
   - フロントエンド用API関数の追加

3. プロジェクト設定の改善
   - package.json: 便利なスクリプト追加（build, start, dev, dev:watch, clean）
   - tsconfig.json: ファイル検索パターンの改善
   - env.example: テスト用パラメータ設定を追加
   - README.md: 詳細な使用方法とセットアップ手順を記載

4. 不要ファイルの削除
   - src/src_test.ts: 不要なテストファイルを削除

5. 依存関係の追加
   - rimraf: ビルドファイルクリーンアップ用

実行コマンド:
npm install rimraf --save-dev
npm run build
npm run dev
npm run dev -- --dishType=4 --dishId=402
npm start

改善結果:
- 材料1～5種類まで対応
- パラメータを柔軟に設定可能（コマンドライン引数、環境変数、デフォルト値）
- フロントエンドから直接呼び出し可能なAPI関数
- 型安全性の向上
- エラーハンドリングの改善
- 開発環境の整備


・データベーステーブル構造の改善とマイグレーション
コミットメッセージ: "feat: データベーステーブル構造の改善とマイグレーション"

実行した内容:
1. データベーステーブル構造の分析と改善
   - 既存テーブル（dish_dqx_craftsman, dish_quantity_dqx）の拡張性問題を特定
   - 材料と数量を列で管理する設計から、中間テーブル方式への移行を決定
   - 新しいテーブル構造（dish_recipe_materials）の設計

2. マイグレーションスクリプトの作成と実行
   - 新しい中間テーブル（dish_recipe_materials_new）の作成
   - 既存データの安全な移行（material_1～5, quantity_1～5 → 中間テーブル）
   - データ整合性の確認とテスト

3. テーブル名の正式化
   - 元のテーブル名をold_プレフィックス付きに変更（バックアップ保持）
   - 新しいテーブル名を正式名称（dish_recipe_materials）に変更
   - 安全なテーブル名変更の実行

4. アプリケーションの更新
   - 新しいテーブル構造に対応したクエリの実装
   - 旧テーブル（old_dish_dqx_craftsman, old_dish_quantity_dqx）を参照するよう修正
   - 新しい機能の追加（料理一覧、材料検索、統計情報）

5. プロジェクト構造の改善
   - ローカル専用スクリプト用のlocal/ディレクトリを作成
   - マイグレーション・テスト・DB操作スクリプトをlocal/に移動
   - .gitignoreでlocal/ディレクトリを除外設定
   - 運用ルールの明記（local/README.md）

実行コマンド:
npm run build
node dist/src/migration.js
node dist/src/migration.js --finalize
node dist/src/index.js --dishType=1 --dishId=101

改善結果:
- データベース設計の拡張性向上（中間テーブル方式）
- 安全なデータ移行とバックアップ保持
- 新しいテーブル構造での柔軟なクエリ実行
- ローカル専用スクリプトの安全な管理
- プロジェクト構造の整理と運用ルールの確立

---
【コミットメッセージ】
feat: フロント・APIサーバー同時起動対応、DB連携、デバッグ構成メモ追加、開発効率向上の整理

【実行内容】
1. concurrently導入、npm run dev:allでフロント・APIサーバー同時起動を実現
2. ベルト一覧をDBから取得し、フロントで表示する機能を実装
3. APIサーバーの雛形作成・DBクエリのテーブル名修正
4. デバッグ構成メモをlocalフォルダに追加
5. VS Codeのlaunch.jsonにnpm run dev:all用デバッグ構成を追加
6. local/配下に開発用メモ・スクリプトを整理
7. その他、開発効率向上のための細かな整理・ドキュメント追記

【主なコマンド例】
npm install -D concurrently
npm run dev:all

---
